# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1
orbs:
  browser-tools: circleci/browser-tools@1.1
jobs:
  build-and-test:
    docker:
      - image: cimg/openjdk:17.0.3-browsers
      - image: circleci/mysql:latest-ram
#reference
        environment:
          MYSQL_ROOT_PASSWORD: ''
          MYSQL_ALLOW_EMPTY_PASSWORD: yes
          MYSQL_DATABASE: Estudiantes2_db
          MYSQL_USER: user

    steps:
      - browser-tools/install-browser-tools
      - checkout
      - run:
      - run:
          name: Install MySQL CLI; Import dummy data; run an example query
          command: |
            sudo apt-get install default-mysql-client
      - run:
          name: Crear Base de datos
          command: |
            
            mysql -h 127.0.0.1 -u root --password='' Estudiantes2_db < sql-data/dummy.sql
            mysql -h 127.0.0.1 -u root --password='' --execute="SELECT * FROM Estudiantes2_db.estudiante"
      - run: 
          name: Build
          command: mvn -B -DAOAlumnoTest clean package
      # Then run your tests!
      #- run:
      #    name: Test
       #   command: mvn -Dtest=DAOAlumnoTest test
# Invoke jobs via workflows
# See: https://circleci.com/docs/2.0/configuration-reference/#workflows
workflows:
  sample: # This is the name of the workflow, feel free to change it to better match your workflow.
    # Inside the workflow, you define the jobs you want to run.
    jobs:
      - build-and-test
      #
